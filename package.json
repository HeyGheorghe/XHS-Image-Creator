{
  "name": "gemini-program",
  "version": "1.0.0",
  "description": "Image generation tool.",
  "scripts": {
    "start": "http-server -p 8080 .",
    "setup": "npm install",
    "generate": "node src/generate_image.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "playwright": "^1.44.1"
  }
}